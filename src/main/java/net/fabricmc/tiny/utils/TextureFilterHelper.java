package net.fabricmc.tiny.utils;

public class TextureFilterHelper {

    public static void setAllSprites(int type)
    {
        /*
        Set<Identifier> keys = DataStorage.SPRITE_ATLASES.keySet();
        for (Identifier key : keys)
        {
            SpriteAtlasTexture atlas = DataStorage.SPRITE_ATLASES.get(key);
            atlas.bindTexture();
            GlStateManager.texParameter(GL11.GL_TEXTURE_2D, GL11.GL_TEXTURE_MIN_FILTER, type);
            GlStateManager.texParameter(GL11.GL_TEXTURE_2D, GL11.GL_TEXTURE_MAG_FILTER, type);
            GL11.glBindTexture(GL11.GL_TEXTURE_2D, 0);
        }
        // TODO
         */
    }

}
